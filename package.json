{
  "name": "ashfall",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=@ashfall/server & npm run dev --workspace=@ashfall/client",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build"
  }
}
